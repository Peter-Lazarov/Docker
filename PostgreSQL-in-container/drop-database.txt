# PostgreSQL, container name - postgres_movement, user - movement, database - movementdb,
docker exec -it postgres_movement bash
psql -U movement -d postgres

# kill all connections to this database
SELECT pg_terminate_backend(pid)
FROM pg_stat_activity
WHERE datname = 'movementdb';

DROP DATABASE movementdb;

# Containers
docker ps -a

    docker rm postgres_movement

# Volumes
docker volume ls

docker volume rm ИМЕТО_НА_VOLUME

docker compose down -v

